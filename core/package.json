{
  "name": "@technium/gardener",
  "version": "0.0.2",
  "description": "A static site generator that works well with LLMs. Opinionated and built for knowledge gardens.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish": "npm version patch && git push && git push --tags && npm publish --access public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.10.0"
}
